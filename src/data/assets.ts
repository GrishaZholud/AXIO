// Optional image URLs. Two ways to supply brand logos / service covers:
//   1) drop files into /public/images/tech/ or /public/images/services/, OR
//   2) paste a URL here (local file always wins if both exist).
// Tip: self-hosting (downloading into /public) is faster and more reliable
// than hotlinking — give the links to the team and they can pull them in.

/** Tool name → logo URL (used by TechBar "Работаем с") */
export const techLogos: Record<string, string> = {
  // 'Tilda': 'https://example.com/tilda.svg',
  // 'WordPress': 'https://...',
};

/** Service slug → cover image URL (used by "Другие направления") */
export const serviceImages: Record<string, string> = {
  // tilda: 'https://example.com/tilda.webp',
  // seo: 'https://...',
};
